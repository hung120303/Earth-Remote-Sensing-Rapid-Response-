# Install the base requirements first. This wheel supports NVIDIA Blackwell GPUs on Linux/WSL.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128; platform_system == "Linux" and platform_machine == "x86_64"
